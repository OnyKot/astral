name: deploy metrics

on:
  push:
    branches:
      - main
    paths:
      - Astral_metrics/**
      - .github/workflows/deploy-metrics.yaml
  workflow_dispatch: {}

concurrency:
  group: deploy-Astral-metrics
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy metrics
    runs-on: blacksmith-2vcpu-ubuntu-2404
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - name: Record deploy commit
        run: |
          set -euo pipefail
          sha=$(git rev-parse HEAD)
          echo "Deploying commit ${sha}"
          printf 'DEPLOY_SHA=%s\n' "$sha" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build image
        uses: docker/build-push-action@v6
        with:
          context: Astral_metrics
          file: Astral_metrics/Dockerfile
          tags: Astral-metrics:${{ env.DEPLOY_SHA }}
          load: true
          platforms: linux/amd64
          cache-from: type=gha,scope=deploy-Astral-metrics
          cache-to: type=gha,mode=max,scope=deploy-Astral-metrics
        env:
          DOCKER_BUILD_SUMMARY: false
          DOCKER_BUILD_RECORD_UPLOAD: false

      - name: Install docker-pussh
        run: |
          set -euo pipefail
          mkdir -p ~/.docker/cli-plugins
          curl -fsSL https://raw.githubusercontent.com/psviderski/unregistry/v0.3.1/docker-pussh \
            -o ~/.docker/cli-plugins/docker-pussh
          chmod +x ~/.docker/cli-plugins/docker-pussh

      - name: Set up SSH agent
        uses: webfactory/ssh-agent@v0.9.1
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY_SERVER }}

      - name: Add server to known hosts
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts

      - name: Push image and deploy
        env:
          IMAGE_TAG: Astral-metrics:${{ env.DEPLOY_SHA }}
          SERVER: ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }}
        run: |
          set -euo pipefail
          docker pussh "${IMAGE_TAG}" "${SERVER}"

          ssh "${SERVER}" "IMAGE_TAG=${IMAGE_TAG} bash" << 'EOF'
          set -euo pipefail
          sudo mkdir -p /opt/Astral-metrics
          sudo chown -R "${USER}:${USER}" /opt/Astral-metrics
          cd /opt/Astral-metrics

          cat > compose.yaml << 'COMPOSEEOF'
          services:
            app:
              image: ${IMAGE_TAG}
              env_file:
                - /etc/Astral/Astral.env
              environment:
                - METRICS_PORT=8080
                - CLICKHOUSE_URL=http://clickhouse:8123
                - CLICKHOUSE_DATABASE=Astral_metrics
                - CLICKHOUSE_USER=Astral
                - ASTRAL_ADMIN_ENDPOINT=https://admin.asrtal.ru
                - ANOMALY_DETECTION_ENABLED=true
              deploy:
                replicas: 1
                restart_policy:
                  condition: on-failure
                  delay: 5s
                  max_attempts: 3
                update_config:
                  parallelism: 1
                  delay: 10s
                  order: start-first
                rollback_config:
                  parallelism: 1
                  delay: 10s
              networks:
                - Astral-shared
              healthcheck:
                test: ['CMD', 'curl', '-f', 'http://localhost:8080/_health']
                interval: 30s
                timeout: 10s
                retries: 3
                start_period: 10s

          networks:
            Astral-shared:
              external: true
          COMPOSEEOF

          docker stack deploy --with-registry-auth --detach=false --resolve-image never -c compose.yaml Astral-metrics
          EOF
